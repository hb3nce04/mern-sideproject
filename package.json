{
	"name": "mern-sideproject",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"build": "cd frontend && npm run build",
		"start": "SET NODE_ENV=production && npm run start --prefix backend",
		"dev": "concurrently \"cd backend && SET NODE_ENV=development && npm run dev\" \"cd frontend && npm run dev\""
	},
	"author": "hb3nce04",
	"license": "ISC",
	"description": "Simple MERN application.",
	"devDependencies": {
		"concurrently": "^9.0.0"
	}
}
